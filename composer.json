{
    "name": "nexus/tax",
    "description": "Framework-agnostic tax calculation engine with multi-jurisdiction support, temporal rate resolution, hierarchical tax structure, reverse charge mechanism, economic nexus determination, partial exemptions, and place-of-supply rules for cross-border services",
    "type": "library",
    "license": "MIT",
    "require": {
        "nexus/common": "*@dev",
        "php": "^8.3",
        "psr/cache": "^3.0",
        "psr/log": "^3.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "psr-4": {
            "Nexus\\Tax\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Nexus\\Tax\\Tests\\": "tests/"
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "suggest": {
        "nexus/accounting": "Optional integration dependency; currently not referenced by package src.",
        "nexus/audit-logger": "Optional integration dependency; currently not referenced by package src.",
        "nexus/geo": "Optional integration dependency; currently not referenced by package src.",
        "nexus/party": "Optional integration dependency; currently not referenced by package src.",
        "nexus/product": "Optional integration dependency; currently not referenced by package src.",
        "nexus/storage": "Optional integration dependency; currently not referenced by package src.",
        "nexus/telemetry": "Optional integration dependency; currently not referenced by package src.",
        "nexus/tenant": "Optional integration dependency; currently not referenced by package src."
    }
}
